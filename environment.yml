name: bci-eeg

channels:
  - conda-forge

dependencies:
  - python=3.11
  - numpy
  - scipy
  - pandas
  - matplotlib
  - mne
  - jupyterlab
  - ipykernel
  - setuptools<81 # Session 11 compatibility constraint; original error not recorded
  - pyserial=3.5
  - pip
  - pip:
      - brainflow==5.22.2
